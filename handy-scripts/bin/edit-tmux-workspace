#!/bin/sh

[ -n "$(command -v nvim)" ] && nvim  +5 -c 'normal f"vi"' ~/.dotfiles/handy-scripts/bin/tmux-sessionizer && exit 0
[ -n "$(command -v vim)" ] && vim  +5 -c 'normal f"vi"' ~/.dotfiles/handy-scripts/bin/tmux-sessionizer && exit 0
